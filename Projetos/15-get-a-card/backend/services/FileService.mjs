import path from 'path'
import fs from 'fs'
import Media from '../models/Media.mjs'

class FileService {
  static create(subdir = '', allowedExtensions = []) {
    
  }

  static delete(subdir = '', filename = '') {
    
  }
}

export default FileService
