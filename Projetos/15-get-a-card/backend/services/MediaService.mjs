import path from 'path'
import fs from 'fs'
import Media from '../models/Media.mjs'

class MediaService {
  static async create(media, subdir = '', allowedExtensions = []) {
    console.log(media)
  }

  static async delete(media) {
    
  }
}

export default MediaService
