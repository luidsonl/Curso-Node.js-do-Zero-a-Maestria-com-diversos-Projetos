<div class="box">
    <h1> {{user.name}} </h1>
    <p> {{user.occupation}} </p>

    <div class="address-list">
        <h2>Lista de endereços</h2>
        {{#each user.addresses}}
            <p># {{this.id}} - {{this.street}} - {{this.number}} - {{this.city}}</p>
        {{/each}}
    </div>
</div>

